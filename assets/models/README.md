# Models Directory

This directory is for 3D model files, including .obj and .fbx formats.

### Naming Conventions
- Use descriptive names for files (e.g., `character.fbx`, `tree.obj`).
- Avoid spaces; use underscores or hyphens instead.